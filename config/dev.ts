
/**
 * 🛠️ DEVELOPMENT & DEBUG CONFIGURATION
 * =====================================
 */

// Set to false to hide the overlay debug panel in the UI.
export const ENABLE_LEDGER_DEBUG = false; 

// In a real environment, this would use process.env.NODE_ENV === 'development'
